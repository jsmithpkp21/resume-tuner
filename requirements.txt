# Application/runtime dependencies
# Dev tools (pre-commit, ruff, black, isort, mypy, types-PyYAML, pytest) live
# in the tooling-managed requirements-dev.txt; do not duplicate them here.

# YAML and TOML parsing (used by scripts)
PyYAML==6.0.3
tomli_w==1.0.0

# Image/font metrics — used by scripts/measure_skills_lines.py (spike #43)
Pillow==12.2.0
# fontTools kern table + TTF parsing (optional kerning precision for #43)
fonttools==4.62.1

# Document export (issue #16)
python-docx==1.2.0
reportlab==4.4.4
