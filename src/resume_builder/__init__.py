"""resume_builder — AI-driven résumé builder runtime package.

Runtime modules (build_resume, jd_ingest, llm_client, etc.) live here. True
helper/ops scripts (validators, env setup, issue tooling) remain in `scripts/`
as thin entrypoints or independent utilities.

See issue #41 for the refactor that established this package.
"""
